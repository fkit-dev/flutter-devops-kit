/// Defines package versions used by FKIT when configuring dependencies.
class PackageVersion {
  const PackageVersion._();

  /// The supported `flutter_launcher_icons` package version.
  static const flutterLauncherIcons = '^0.14.4';

  /// The supported `flutter_native_splash` package version.
  static const flutterNativeSplash = '^2.4.6';

  /// The supported `freezed` package version.
  static const freezed = '^3.2.3';

  /// The supported `build_runner` package version.
  static const buildRunner = '^2.5.4';
}
