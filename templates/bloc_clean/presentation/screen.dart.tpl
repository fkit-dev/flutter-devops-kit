import 'package:flutter/material.dart';

class {{screen}} extends StatelessWidget {
  const {{screen}}({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          '{{featurePascal}} Screen',
        ),
      ),
    );
  }
}